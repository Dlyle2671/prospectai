version: 1
appRoot: prospectai-next
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  cache:
    paths:
      - node_modules/**/*
